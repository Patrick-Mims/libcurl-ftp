#include <pthread.h>
#include <stdio.h>
#include <stdlib.h>

#include "lc.h"

static void *threadFtp(void *arg)
{
    // struct Node *type = (struct Node *)arg;
    // stack_t type = (stack_t)arg;

    return NULL;
}
